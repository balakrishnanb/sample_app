<li>
  <span class="content"><%= link_to question.title, question %></span>
  <span class="timestamp">
    Asked <%= time_ago_in_words(question.created_at) %> ago by <%= link_to question.user.name, question.user %>
  </span>
</li>

